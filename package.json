{
    "name": "hostel-navigator",
    "version": "1.0.0",
    "description": "A hostel navigation app using Leaflet and PostgreSQL",
    "main": "server.js",
    "scripts": {
      "start": "node server.js"
    },
    "dependencies": {
      "express": "^4.17.1",             // If you're using Express.js for the server
      "leaflet": "^1.7.1",              // Leaflet for interactive maps
      "jquery": "^3.6.0",               // jQuery for DOM manipulation and AJAX
      "axios": "^0.21.1",               // Optional: for handling HTTP requests on the server
      "pg": "^8.7.1",                   // PostgreSQL client to connect to the database
      "fuzzywuzzy": "^1.1.0",           // Fuzzy string matching for suggestions
      "fuzzywuzzy-js": "^0.3.1"         // A JavaScript version of fuzzy matching (if needed on client side)
    },
    "devDependencies": {},
    "engines": {
      "node": ">=14.0.0"                // Specify Node.js version
    },
    "author": "Your Name",
    "license": "ISC"
  }
  